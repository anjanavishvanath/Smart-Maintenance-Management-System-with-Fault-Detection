# Frontend environment variables (read by Vite at build time).
# Copy to `.env` (or `.env.production`) and adjust per environment.
# DO NOT commit real `.env` files.

# Base URL of the PreSense backend API. No trailing slash.
VITE_API_URL=http://localhost:5000
